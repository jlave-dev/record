{
  "name": "capture",
  "version": "0.3.0",
  "description": "Native ScreenCaptureKit macOS application recording CLI.",
  "bin": {
    "capture": "./dist/capture"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "./scripts/build.sh",
    "build:binary:macos-arm64": "npm run build && mkdir -p dist/binary && cp dist/capture dist/binary/capture-macos-arm64",
    "capture": "./dist/capture",
    "doctor": "./dist/capture doctor",
    "install:local": "./scripts/install-local.sh",
    "setup": "npm run build && npm run install:local && $HOME/.local/bin/capture setup && $HOME/.local/bin/capture doctor",
    "test": "swift test --package-path native"
  },
  "engines": {
    "node": ">=22"
  }
}
