{
  "name": "transcribe",
  "version": "0.4.0",
  "description": "Local whisper.cpp media transcription CLI.",
  "type": "commonjs",
  "bin": {
    "transcribe": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json --emitDeclarationOnly && esbuild src/index.ts --bundle --platform=node --format=cjs --target=node22 --sourcemap --outfile=dist/index.js && chmod +x dist/index.js",
    "build:binary:macos-arm64": "./scripts/build-binary.sh",
    "transcribe": "node dist/index.js",
    "doctor": "node dist/index.js doctor",
    "install:local": "../../scripts/install-local.sh \"$PWD\" transcribe",
    "setup": "npm run build && node dist/index.js setup && npm run install:local && node dist/index.js doctor",
    "test": "npm run build && ./scripts/test-setup.sh"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=22"
  }
}
