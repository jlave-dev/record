interface:
  display_name: "Capture"
  short_description: "Record Mac apps locally with ScreenCaptureKit."
  default_prompt: "Use $capture to record a visible Mac app."

policy:
  allow_implicit_invocation: true
